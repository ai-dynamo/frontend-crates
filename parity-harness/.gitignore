# Ephemeral staging tree built by run.sh (copies of vendored python + symlinks
# to this repo's fixtures/parser source). Never committed.
.stage/
# Rendered parity matrix — a local artifact, regenerate with `run.sh table`.
PARITY*.html
# Python bytecode from running the vendored generator / validate.py
__pycache__/
*.pyc
