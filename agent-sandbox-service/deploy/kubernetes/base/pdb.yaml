apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: agent-rt-sandbox-service
  namespace: agent-rt-sandbox-system
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-rt-sandbox-service
