# Stub dependency list for conformance capture helpers. Keep this in sync with the engine versions used to refresh fixtures.
# Staged as pyproject.toml; the generator's _peer_versions() text-searches it.
[tool.parity]
peers = [
  "sglang[diffusion]==0.5.12.post1",
  "vllm[flashinfer,runai,otel]==0.22.0",
]
