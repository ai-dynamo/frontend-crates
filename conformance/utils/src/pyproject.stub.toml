# Source of truth for conformance peer engine versions.
# Staged as pyproject.toml; the generator's _peer_versions() text-searches it.
[tool.parity]
peers = [
  "sglang[diffusion]==0.5.14",
  "vllm[flashinfer,runai,otel]==0.26.0",
]
