corpus: stream
family: llama3_json
capture: vllm_rust-0.25.1
documents:
  TOOLCALLING.streamv1.4.yaml:
    family: llama3_json
    mode: streamv1
    captured_with:
      vllm_rust: 0.25.1
    cases:
      TOOLCALLING.streamv1.4.e:
        exception: 'ToolParserError::ParsingFailed (near "{\"name\": \"get_weather\", \"arguments\":": invalid Llama JSON

          expected `parameters`)'
