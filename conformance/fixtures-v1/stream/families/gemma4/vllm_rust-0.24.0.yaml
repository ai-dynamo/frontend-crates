corpus: stream
family: gemma4
capture: vllm_rust-0.24.0
documents:
  TOOLCALLING.streamv1.4.yaml:
    family: gemma4
    mode: streamv1
    captured_with:
      vllm_rust: 0.24.0
    cases:
      TOOLCALLING.streamv1.4.a:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
      TOOLCALLING.streamv1.4.b:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
      TOOLCALLING.streamv1.4.c:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
      TOOLCALLING.streamv1.4.d:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
  TOOLCALLING.streamv1.5.yaml:
    family: gemma4
    mode: streamv1
    captured_with:
      vllm_rust: 0.24.0
    cases:
      TOOLCALLING.streamv1.5.a:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
      TOOLCALLING.streamv1.5.c:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
      TOOLCALLING.streamv1.5.d:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
      TOOLCALLING.streamv1.5.e:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
  TOOLCALLING.streamv1.6.yaml:
    family: gemma4
    mode: streamv1
    captured_with:
      vllm_rust: 0.24.0
    cases:
      TOOLCALLING.streamv1.6.c:
        unavailable: gemma4 moved to the native unified parser in vLLM 0.25.0; not exposed via the tool::ToolParser probe.
