corpus: stream
family: deepseek_v4
capture: vllm_rust-0.24.0
documents:
  TOOLCALLING.streamv1.4.yaml:
    family: deepseek_v4
    mode: streamv1
    captured_with:
      vllm_rust: 0.24.0
    cases:
      TOOLCALLING.streamv1.4.a:
        exception: ToolParserError::ParsingFailed ()
      TOOLCALLING.streamv1.4.c:
        exception: ToolParserError::ParsingFailed ()
      TOOLCALLING.streamv1.4.d:
        exception: ToolParserError::ParsingFailed ()
  TOOLCALLING.streamv1.5.yaml:
    family: deepseek_v4
    mode: streamv1
    captured_with:
      vllm_rust: 0.24.0
    cases:
      TOOLCALLING.streamv1.5.a:
        exception: ToolParserError::ParsingFailed (incomplete DeepSeek DSML tool call)
      TOOLCALLING.streamv1.5.c:
        exception: ToolParserError::ParsingFailed (incomplete DeepSeek DSML tool call)
      TOOLCALLING.streamv1.5.d:
        exception: ToolParserError::ParsingFailed (incomplete DeepSeek DSML tool call)
      TOOLCALLING.streamv1.5.e:
        exception: ToolParserError::ParsingFailed (incomplete DeepSeek DSML tool call)
