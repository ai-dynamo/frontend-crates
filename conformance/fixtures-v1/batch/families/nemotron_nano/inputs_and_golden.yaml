corpus: batch
family: nemotron_nano
capture: inputs
documents:
  TOOLCALLING.batch.2.yaml:
    family: nemotron_nano
    mode: batch
    cases:
      TOOLCALLING.batch.2.a:
        description: Parallel calls (back-to-back tool_call wrappers)
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>

          <tool_call>

          <function=get_time>

          <parameter=timezone>

          EST

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
        - name: get_time
          parameters:
            type: object
            properties:
              timezone:
                type: string
      TOOLCALLING.batch.2.c:
        description: With surrounding narration
        ref: dynamo
        model_text: 'Both: <tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>

          <tool_call>

          <function=get_time>

          <parameter=timezone>

          EST

          </parameter>

          </function>

          </tool_call> Done.'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
        - name: get_time
          parameters:
            type: object
            properties:
              timezone:
                type: string
      TOOLCALLING.batch.2.d:
        description: Same-name twice
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>

          <tool_call>

          <function=get_weather>

          <parameter=location>

          LA

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.2.b:
        description: Two back-to-back fence pairs — not applicable to this family
        explanation: nemotron_nano's multi-call form is exercised by TOOLCALLING.batch.2.a / 2.c / 2.d; no back-to-back fence/wrapper pattern applies to this family's parser syntax.
  TOOLCALLING.batch.4.yaml:
    family: nemotron_nano
    mode: batch
    cases:
      TOOLCALLING.batch.4.a:
        description: Tool_call wrapper with no <function> body
        ref: dynamo
        model_text: '<tool_call>

          nonsense

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.4.d:
        description: Missing </parameter> closing tag
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.4.b:
        description: Malformed variant 4.b — not yet authored for this family
        explanation: Malformed-input variant 4.b not yet authored for nemotron_nano; see 4.a / 4.c / 4.d for this family's malformed-input scenarios.
      TOOLCALLING.batch.4.c:
        description: Malformed variant 4.c — not yet authored for this family
        explanation: Malformed-input variant 4.c not yet authored for nemotron_nano; see 4.a / 4.b / 4.d for this family's malformed-input scenarios.
      TOOLCALLING.batch.4.e:
        description: Malformed-prefix recovery — n/a for this family's syntax
        explanation: 'TOOLCALLING.batch.4.e is specific to llama3_json''s raw JSON-object scanner: a malformed JSON-looking prefix is followed by a complete JSON object, and the detector may resynchronize on the later object. This family uses a framed parser grammar instead of that raw JSON-prefix scanner; malformed-input coverage for this family lives in TOOLCALLING.batch.4.a through 4.d.'
      TOOLCALLING.batch.4.f:
        description: Tool-name XML tag case - n/a for this family's syntax
        explanation: TOOLCALLING.batch.4.f is specific to Qwen3-Coder XML syntax, where the model may emit a tool-name tag such as <terminal> inside <tool_call> instead of the required <function=Terminal> opener. This family does not use Qwen3-Coder's <function=...> opener.
  TOOLCALLING.batch.5.yaml:
    family: nemotron_nano
    mode: batch
    cases:
      TOOLCALLING.batch.5.a:
        description: Missing </tool_call> end marker
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.5.b:
        description: Closing </tool_call> without matching <tool_call> open
        ref: dynamo
        model_text: '<function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.5.c:
        description: Truncation mid-parameter value
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NY'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.5.d:
        description: Multi-call, last call missing only end marker (body complete)
        ref: dynamo
        model_text: 'I''ll start by fetching the weather for both Boston and New York at the same time!

          <tool_call>

          <function=get_weather>

          <parameter=location>

          Boston

          </parameter>

          </function>

          </tool_call>

          <tool_call>

          <function=get_weather>

          <parameter=location>

          New York

          </parameter>

          </function>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.5.e:
        description: Multi-call, last call truncated mid-arg-value
        ref: dynamo
        model_text: 'I''ll start by fetching the weather for both Boston and New York at the same time!

          <tool_call>

          <function=get_weather>

          <parameter=location>

          Boston

          </parameter>

          </function>

          </tool_call>

          <tool_call>

          <function=get_weather>

          <parameter=location>

          New York'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
  TOOLCALLING.batch.6.yaml:
    family: nemotron_nano
    mode: batch
    cases:
      TOOLCALLING.batch.6.a:
        description: Function block with no <parameter>
        ref: dynamo
        model_text: '<tool_call>

          <function=get_time>

          </function>

          </tool_call>'
        tools:
        - name: get_time
          parameters:
            type: object
            properties: {}
      TOOLCALLING.batch.6.b:
        description: Function block with extra whitespace inside
        ref: dynamo
        model_text: '<tool_call>

          <function=get_time>


          </function>

          </tool_call>'
        tools:
        - name: get_time
          parameters:
            type: object
            properties: {}
      TOOLCALLING.batch.6.c:
        description: Empty-body variant 6.c — not yet authored for this family
        explanation: Empty-body variant 6.c not yet authored for nemotron_nano; see 6.a / 6.b for this family's empty / whitespace scenarios.
  TOOLCALLING.batch.7.yaml:
    family: nemotron_nano
    mode: batch
    cases:
      TOOLCALLING.batch.7.a:
        description: Multiple parameters
        ref: dynamo
        model_text: '<tool_call>

          <function=book_flight>

          <parameter=destination>

          Paris

          </parameter>

          <parameter=passengers>

          2

          </parameter>

          <parameter=first_class>

          true

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: book_flight
          parameters:
            type: object
            properties:
              destination:
                type: string
              passengers:
                type: integer
              first_class:
                type: boolean
      TOOLCALLING.batch.7.b:
        description: Unicode in parameter
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          Tōkyō central

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.7.c:
        description: Schema-type-mismatch test 7.c — not yet authored for this family
        explanation: Schema-type-mismatch test 7.c not yet authored for nemotron_nano; argument-shape coverage is provided by 7.a / 7.b for this family.
      TOOLCALLING.batch.7.d:
        description: Nested-arguments test 7.d — not yet authored for this family
        explanation: Nested-arguments test 7.d not yet authored for nemotron_nano; see 7.a / 7.b for simpler argument shapes.
      TOOLCALLING.batch.7.e:
        description: Large / deep JSON-edge payload test 7.e — not yet authored for this family
        explanation: Large/deep JSON-edge payload test 7.e not yet authored for nemotron_nano; existing batch.7 coverage remains in 7.a-d.
      TOOLCALLING.batch.7.f:
        description: Numeric precision edge preserves integer-like number literal
        ref: inferhub live capture on nvidia/nvidia/Nemotron-3-Nano-30B-A3B; format follows https://huggingface.co/Qwen/Qwen3-Coder-480B-A35B-Instruct/blob/main/qwen3coder_tool_parser.py
        model_text: '<tool_call>

          <function=set_limit>

          <parameter=count>

          9007199254740993

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: set_limit
          parameters:
            type: object
            properties:
              count:
                type: number
  TOOLCALLING.batch.8.yaml:
    family: nemotron_nano
    mode: batch
    cases:
      TOOLCALLING.batch.8.a:
        description: Narration before tool call only
        ref: dynamo
        model_text: 'I will check the weather. <tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.8.b:
        description: Narration after tool call only
        ref: dynamo
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call> Let me know if you need more.'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.8.c:
        description: Narration both before and after (sandwich)
        ref: dynamo
        model_text: 'I will check the weather. <tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call> Let me know if you need more.'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.8.d:
        description: Narration between multiple tool calls
        ref: dynamo
        model_text: 'I will check the weather. <tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call> Then check LA weather. <tool_call>

          <function=get_weather>

          <parameter=location>

          LA

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
  TOOLCALLING.batch.yaml:
    family: nemotron_nano
    model_label: Nemotron Nano v3
    mode: batch
    cases:
      TOOLCALLING.batch.1:
        description: Single tool call (happy path)
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.13.c:
        description: Mixed known and unknown calls not applicable
        explanation: This case is not applicable to nemotron_nano because neither vLLM nor SGLang has a peer parser for this family.
      TOOLCALLING.batch.3:
        description: No tool call (plain text)
        model_text: Hello, how can I help you today?
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.9.a:
        description: Empty model text
        model_text: ''
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.9.b:
        description: Blank / whitespace-only model text
        ref: dynamo
        model_text: '   '
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.10:
        description: Duplicate calls (same name twice)
        model_text: '<tool_call>

          <function=get_weather>

          <parameter=location>

          NYC

          </parameter>

          </function>

          </tool_call>

          <tool_call>

          <function=get_weather>

          <parameter=location>

          LA

          </parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.batch.30:
        description: Separator-character case not applicable
        explanation: This case is not applicable to nemotron_nano because it is a qwen3_coder-style XML parser with no vLLM/SGLang peer parser.
      TOOLCALLING.batch.31:
        description: Multi-call separator-character case not applicable
        explanation: This case is not applicable to nemotron_nano because it is a qwen3_coder-style XML parser with no vLLM/SGLang peer parser.
      TOOLCALLING.batch.13:
        description: Unknown tool name absent from supplied tools
        ref: dynamo
        model_text: '<tool_call>

          <function=unknown_tool>

          <parameter=location>NYC</parameter>

          </function>

          </tool_call>'
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
  TOOLCALLING.stream.1.yaml:
    family: nemotron_nano
    model_label: Nemotron Nano v3
    mode: stream
    cases:
      TOOLCALLING.stream.1.a:
        description: Complete tool-call payload delivered in one content chunk
        ref: derived from TOOLCALLING.batch.1
        chunks:
        - delta_text: '<tool_call>

            <function=get_weather>

            <parameter=location>

            NYC

            </parameter>

            </function>

            </tool_call>'
        - delta_text: ''
          finish_reason: tool_calls
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.stream.1.b:
        description: Complete tool call split across parser-significant boundaries
        ref: derived from TOOLCALLING.batch.1
        chunks:
        - delta_text: '<tool_call>

            '
        - delta_text: '<function=get_weather>

            '
        - delta_text: '<parameter=location>

            '
        - delta_text: 'NYC

            '
        - delta_text: '</parameter>

            '
        - delta_text: '</function>

            '
        - delta_text: </tool_call>
        - delta_text: ''
          finish_reason: tool_calls
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
  TOOLCALLING.stream.2.yaml:
    family: nemotron_nano
    model_label: Nemotron Nano v3
    mode: stream
    cases:
      TOOLCALLING.stream.2:
        description: Multiple tool calls split across multiple content chunks
        ref: derived from TOOLCALLING.batch.2.a
        chunks:
        - delta_text: <tool_cal
        - delta_text: 'l>

            <fun'
        - delta_text: ction=get_wea
        - delta_text: ther>
        - delta_text: '

            <parameter=locat'
        - delta_text: ion
        - delta_text: '>

            NYC

            </p'
        - delta_text: aramete
        - delta_text: 'r>

            </function'
        - delta_text: '>

            </t'
        - delta_text: 'ool_call>

            <tool_c'
        - delta_text: all
        - delta_text: '>

            <functi'
        - delta_text: on=get_
        - delta_text: 'time>

            <parame'
        - delta_text: ter=t
        - delta_text: 'imezone>

            EST

            </pa'
        - delta_text: ram
        - delta_text: 'eter>

            </f'
        - delta_text: unction
        - delta_text: '>

            </tool_call'
        - delta_text: '>'
        - delta_text: ''
          finish_reason: tool_calls
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
        - name: get_time
          parameters:
            type: object
            properties:
              timezone:
                type: string
  TOOLCALLING.stream.3.yaml:
    family: nemotron_nano
    model_label: Nemotron Nano v3
    mode: stream
    cases:
      TOOLCALLING.stream.3:
        description: Complete single tool call with a grammar token split across chunk boundaries
        ref: derived from TOOLCALLING.batch.1
        chunks:
        - delta_text: <t
        - delta_text: ool
        - delta_text: _call
        - delta_text: '>

            <function'
        - delta_text: '=get_weather>

            <para'
        - delta_text: me
        - delta_text: ter
        - delta_text: =loca
        - delta_text: 'tion>

            NYC

            <'
        - delta_text: '/parameter>

            </funct'
        - delta_text: io
        - delta_text: 'n>

            '
        - delta_text: </too
        - delta_text: l_call>
        - delta_text: ''
          finish_reason: tool_calls
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
  TOOLCALLING.stream.4.yaml:
    family: nemotron_nano
    model_label: Nemotron Nano v3
    mode: stream
    cases:
      TOOLCALLING.stream.4.a:
        description: Truncated stream terminates after a complete in-flight tool-call body
        ref: derived from TOOLCALLING.batch.5.a
        chunks:
        - delta_text: '<tool_call>

            <function=get_weather>

            <parameter=location>

            NYC

            </parameter>

            </function>'
        - delta_text: ''
          finish_reason: stop
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.stream.4.b:
        description: Stream terminates mid-call body before the in-flight argument payload is complete
        ref: derived from TOOLCALLING.batch.5.c
        chunks:
        - delta_text: '<tool_call>

            <function=get_wea'
        - delta_text: 'ther>

            <parameter=location>

            NY'
        - delta_text: ''
          finish_reason: stop
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
      TOOLCALLING.stream.4.c:
        description: Stream reaches length after a valid inner tool-call body without the required outer wrapper, followed by repeated close markers
        ref: derived from nemotron-ultra-preview-tool-call-leak-repro/repro_tool_call_leak.py
        chunks:
        - delta_text: '<function=get_weather>

            <parameter=location>

            NYC

            </parameter>

            </function>

            </function>

            </function>'
        - delta_text: ''
          finish_reason: length
        tools:
        - name: get_weather
          parameters:
            type: object
            properties:
              location:
                type: string
