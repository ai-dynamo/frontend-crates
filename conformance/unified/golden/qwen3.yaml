# Golden (spec-derived) unified event cases for the qwen3 grammar.
#
# GENERATED by conformance/utils/src/gen_unified_golden.py from ONE scenario
# spec -- do not edit by hand; edit the spec so every family stays in lockstep.
# GOLDEN is the AUTHORED correctness oracle (what a correct UnifiedParser MUST
# emit), reasoned from UNIFIED_CASES.md -- NOT captured from any implementation.
# qwen3 grammar: reasoning `<think>...</think>`, tool `<tool_call><function=NAME><parameter=KEY>VALUE</parameter></function></tool_call>`.
version: 1
family: qwen3
cases:
  UNIFIED.arg_marker_in_string.qwen3:
    description: "A close-marker-looking sequence INSIDE a string arg value. Invariant I7 — the value is data, preserved exactly, not truncated at the marker-looking substring."
    policy: []
    input: |-
      <tool_call>
      <function=run>
      <parameter=cmd>
      git log </tool_call> --oneline
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "run", "arguments": {"cmd": "git log </tool_call> --oneline"}}]
    expect: {"vllm": {"verdict": "match", "note": "hypothesis: value ends at the real `\\n</parameter>`; an embedded `</tool_call>` is data. Verify at capture time"}, "dynamo": {"verdict": "match", "note": "v2 reads the parameter value up to `</parameter>`; embedded `</tool_call>` preserved"}}
  UNIFIED.arg_unicode.qwen3:
    description: "Unicode + spaces in a string argument value (streamv2.7.b). Preserved exactly (I7)."
    policy: []
    input: |-
      <tool_call>
      <function=get_weather>
      <parameter=city>
      São Paulo 東京
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {"city": "São Paulo 東京"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.content_then_reason.qwen3:
    description: "Visible content, then reasoning, then more content. The split hoists reasoning to the front and merges the two content spans."
    policy: []
    input: |-
      Hello there. <think>let me recall</think>The capital is Paris.
    golden: [{"kind": "text", "text": "Hello there. "}, {"kind": "reasoning", "text": "let me recall"}, {"kind": "text", "text": "The capital is Paris."}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "ORDER", "note": "reasoning hoisted ahead of leading content; the two text spans merge"}}
  UNIFIED.content_then_reason_then_tool.qwen3:
    description: "Visible content BEFORE reasoning, then a tool call. The split hoists all reasoning to the front, so content-before-reasoning loses order."
    policy: []
    input: |-
      Sure, one sec. <think>checking the forecast</think><tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "text", "text": "Sure, one sec. "}, {"kind": "reasoning", "text": "checking the forecast"}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "ORDER", "note": "reasoning hoisted ahead of the leading content"}}
  UNIFIED.empty_args.qwen3:
    description: "A tool call with an empty argument object {} (streamv2.6.a). Policy P3 — empty args serialize to {}."
    policy: ["P3"]
    input: |-
      <tool_call>
      <function=get_weather>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.interstitial_text.qwen3:
    description: "Reasoning, then visible text, THEN a tool call. Text between reasoning-end and the call must survive as its own event, in order."
    policy: []
    input: |-
      <think>a</think>Here you go: <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "reasoning", "text": "a"}, {"kind": "text", "text": "Here you go: "}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.narrated_calls.qwen3:
    description: "Multiple tool calls with visible narration between each — tool_call -> text -> tool_call -> text -> tool_call. The agentic pattern: call, narrate, call again. Every call and every inter-call text span must surface as its own ordered event."
    policy: []
    input: |-
      <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call> then I'll run <tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call> and <tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "text", "text": " then I'll run "}, {"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "text", "text": " and "}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.orphan_close_after_prose.qwen3:
    description: "Prose followed by an orphan close marker with no matching open. Best-effort recovery — the prose stays as content, the orphan marker is stripped, nothing leaks."
    policy: []
    input: |-
      I will check that. </tool_call>
    golden: [{"kind": "text", "text": "I will check that. "}]
    expect: {"vllm": {"verdict": "match", "note": "hypothesis: an orphan `</tool_call>` with no open is stripped. Verify at capture time"}, "dynamo": {"verdict": "match", "note": "hypothesis: v2 qwen3 strips the orphan close. Verify at capture time"}}
  UNIFIED.reason_after_tool.qwen3:
    description: "Reasoning AFTER a tool call, then final text (Example A). The split cannot represent reasoning between the call and the answer."
    policy: []
    input: |-
      <think>Look it up.</think><tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call><think>Now answer.</think>It's 18C.
    golden: [{"kind": "reasoning", "text": "Look it up."}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "reasoning", "text": "Now answer."}, {"kind": "text", "text": "It's 18C."}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "v1 reasoning runs over the whole stream first -> both think spans merge into one event ahead of the tool_call"}}
  UNIFIED.reason_between_calls.qwen3:
    description: "Reasoning BETWEEN two tool calls with no surrounding text — the tightest interleave."
    policy: []
    input: |-
      <tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call><think>mid</think><tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "reasoning", "text": "mid"}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "batch v1 hoists the mid-call reasoning ahead of both calls"}}
  UNIFIED.reason_interleaved.qwen3:
    description: "reason -> tool -> reason -> tool. Two calls, each preceded by its own thought."
    policy: []
    input: |-
      <think>A</think><tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call><think>B</think><tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "reasoning", "text": "A"}, {"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "reasoning", "text": "B"}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "both think spans merge up front, ahead of both calls"}}
  UNIFIED.reason_markup_in_arg.qwen3:
    description: "'Tool call contains reasoning' — a reasoning-channel marker sits inside a QUOTED tool-arg value. This is NOT a leak: a leak is control markup surfacing in visible content or reasoning, but here the markup is a tool ARGUMENT VALUE (data bound for the function, inside the grammar's string delimiters), so by I7 the parser preserves it byte-exact. The gemma4 native UnifiedParser confirms this golden exactly. Failure mode: a reasoning-first pipeline extracts the `<think>`/`<|channel>` from inside the arg BEFORE tool parsing, hoisting it into a spurious reasoning event and corrupting the arg to empty."
    policy: []
    input: |-
      <tool_call>
      <function=log>
      <parameter=note>
      <think>reconsider</think>
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "log", "arguments": {"note": "<think>reconsider</think>"}}]
    expect: {"vllm": {"verdict": "diverge", "class": "ARG_MISMATCH", "note": "hypothesis: the `<think>...</think>` inside the parameter value is extracted as reasoning first, corrupting the arg. Verify at capture time"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "hypothesis: v1 reasoning lifts the embedded `<think>` out of the arg. Verify at capture time"}}
  UNIFIED.reason_markup_in_arg_with_text.qwen3:
    description: "reason_markup_in_arg (tool arg value contains reasoning markup, I7 data) WITH visible narration before and after the call. All three channels at once: leading text -> tool call whose arg holds reasoning markup -> trailing text. Golden keeps the visible text as text, the call as a call, and the markup byte-exact in the arg. A reasoning-first pipeline both corrupts the arg (extracting the embedded reasoning) and can reorder/misroute the surrounding text."
    policy: []
    input: |-
      Logging now: <tool_call>
      <function=log>
      <parameter=note>
      <think>reconsider</think>
      </parameter>
      </function>
      </tool_call> done.
    golden: [{"kind": "text", "text": "Logging now: "}, {"kind": "tool_call", "name": "log", "arguments": {"note": "<think>reconsider</think>"}}, {"kind": "text", "text": " done."}]
    expect: {"vllm": {"verdict": "diverge", "class": "ARG_MISMATCH", "note": "hypothesis: the `<think>` inside the parameter value is extracted as reasoning first, corrupting the arg. Verify at capture time"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "hypothesis: v1 reasoning lifts the embedded `<think>` out of the arg and ahead of the text. Verify at capture time"}}
  UNIFIED.reason_only.qwen3:
    description: "A reasoning span with no visible answer and no tool call (REASONING.2.a)."
    policy: []
    input: |-
      <think>just thinking, no answer</think>
    golden: [{"kind": "reasoning", "text": "just thinking, no answer"}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.reason_then_content.qwen3:
    description: "Reasoning then visible content, no tool call (baseline)."
    policy: []
    input: |-
      <think>let me think</think>The answer is 42.
    golden: [{"kind": "reasoning", "text": "let me think"}, {"kind": "text", "text": "The answer is 42."}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.reason_then_tool.qwen3:
    description: "Reasoning fully precedes one tool call (baseline)."
    policy: []
    input: |-
      <think>Check weather.</think><tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "reasoning", "text": "Check weather."}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.reason_tool_reason_tool_reason.qwen3:
    description: "reason -> tool -> reason -> tool -> reason. Three reasoning spans around two calls, including reasoning AFTER the last call — the split cannot place any of them."
    policy: []
    input: |-
      <think>A</think><tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call><think>B</think><tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call><think>C</think>
    golden: [{"kind": "reasoning", "text": "A"}, {"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "reasoning", "text": "B"}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}, {"kind": "reasoning", "text": "C"}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "batch v1 reasoning merges A+B+C into one event ahead of both calls"}}
  UNIFIED.reason_tool_text_reason_tool.qwen3:
    description: "reason -> tool -> text -> reason -> tool. Two reasoning spans separated by a call and text."
    policy: []
    input: |-
      <think>A</think><tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call>working on it<think>B</think><tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "reasoning", "text": "A"}, {"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "text", "text": "working on it"}, {"kind": "reasoning", "text": "B"}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "reasoning A and B merge up front; the second reasoning span loses its position"}}
  UNIFIED.reason_unterminated.qwen3:
    description: "Stream ends while still inside reasoning (no close marker). Open reasoning is promoted at finish, not dropped and not leaked as text."
    policy: []
    input: |-
      <think>thinking but stream ends
    golden: [{"kind": "reasoning", "text": "thinking but stream ends"}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match", "note": "verify against v1 qwen3 reasoning finish() at capture time"}}
  UNIFIED.text_before_tool.qwen3:
    description: "Visible text before a single tool call, no reasoning (streamv2.8.a)."
    policy: []
    input: |-
      On it: <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "text", "text": "On it: "}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.text_between_calls.qwen3:
    description: "Visible text between two tool calls (streamv2.8.d)."
    policy: []
    input: |-
      <tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call> then <tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "text", "text": " then "}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.text_only.qwen3:
    description: "Plain answer, no reasoning and no tool call (streamv2.3). Pure content passthrough."
    policy: []
    input: |-
      The answer is 42, no tools needed.
    golden: [{"kind": "text", "text": "The answer is 42, no tools needed."}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.text_reason_tool_text_reason_tool.qwen3:
    description: "Deep well-formed interleave — visible text, reasoning, and tool calls alternating (text -> reason -> tool -> text -> reason -> tool). Every segment must survive in emitted order; the point is that user text, reasoning, and calls all mix in one stream."
    policy: []
    input: |-
      Sure. <think>check A</think><tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call> and <think>check B</think><tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "text", "text": "Sure. "}, {"kind": "reasoning", "text": "check A"}, {"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "text", "text": " and "}, {"kind": "reasoning", "text": "check B"}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "batch v1 reasoning hoists both think spans ahead of everything; the interleaved text/call order collapses"}}
  UNIFIED.text_sandwich.qwen3:
    description: "Visible text both before and after a tool call (streamv2.8.c)."
    policy: []
    input: |-
      Before. <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call> After.
    golden: [{"kind": "text", "text": "Before. "}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "text", "text": " After."}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.tool_in_reason.qwen3:
    description: "'Reasoning contains tool call' — a well-formed tool-call envelope nested INSIDE a reasoning span. This is the OPPOSITE of reason_markup_in_arg: a reasoning span is opaque TEXT, not a quoted data region, so a real tool-call marker inside it IS structural. Best-effort recovery breaks out of reasoning, emits the call, and resumes reasoning after its close (golden: reason -> call -> reason). Leaking the raw `<|tool_call>...<tool_call|>` into reasoning_content, or dropping the call, is the regression — which is what every reasoning-first engine does here."
    policy: []
    input: |-
      <think>I should check. <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call> now answer</think>
    golden: [{"kind": "reasoning", "text": "I should check. "}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "reasoning", "text": " now answer"}]
    expect: {"vllm": {"verdict": "diverge", "class": "LEAK", "note": "hypothesis: the `</think>` closes only after the nested call, so the tool markup leaks into reasoning and the call is dropped. Verify at capture time"}, "dynamo": {"verdict": "diverge", "class": "LEAK", "note": "hypothesis: v1 reasoning consumes to `</think>`, leaking the nested tool markup. Verify at capture time"}}
  UNIFIED.tool_in_reason_with_text.qwen3:
    description: "tool_in_reason (a tool call nested inside a reasoning span, break-out recovery) WITH visible narration before and after the reasoning span. All three channels at once: leading text -> reasoning that wraps a real call -> trailing text. Golden: text -> reason -> call -> reason -> text. Engines that treat reasoning as opaque-until-close leak the nested tool markup into reasoning_content and drop the call."
    policy: []
    input: |-
      Sure. <think>I should check. <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call> now answer</think> Here you go.
    golden: [{"kind": "text", "text": "Sure. "}, {"kind": "reasoning", "text": "I should check. "}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "reasoning", "text": " now answer"}, {"kind": "text", "text": " Here you go."}]
    expect: {"vllm": {"verdict": "diverge", "class": "LEAK", "note": "hypothesis: `</think>` closes only after the nested call, so the tool markup leaks into reasoning and the call is dropped. Verify at capture time"}, "dynamo": {"verdict": "diverge", "class": "LEAK", "note": "hypothesis: v1 reasoning consumes to `</think>`, leaking the nested tool markup. Verify at capture time"}}
  UNIFIED.tool_no_close.qwen3:
    description: "A single tool call whose body is complete but the close marker never arrives before EOF (streamv2.5.a). Best-effort recovery emits the complete call at finish."
    policy: []
    input: |-
      <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}]
    expect: {"vllm": {"verdict": "match", "note": "body complete; recover at finish"}, "dynamo": {"verdict": "match", "note": "hypothesis: v2 recovers; verify at capture"}}
  UNIFIED.tool_only.qwen3:
    description: "Single tool call, no reasoning. Must stay green everywhere (the existing tool suite's world)."
    policy: []
    input: |-
      <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.trailing_text_after_tool.qwen3:
    description: "Arbitrary visible prose AFTER the tool call (the point is it could be ANY content, so it must survive). Policy P1 (best-effort recovery) — trailing model text is preserved, not suppressed."
    policy: ["P1"]
    input: |-
      <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call>The forecast shows clear skies for the rest of the week.
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "text", "text": "The forecast shows clear skies for the rest of the week."}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.truncated_tool_eof.qwen3:
    description: "Stream ends mid tool call (no close marker). Policy P2 — drop the incomplete call, keep valid preceding output, no error, no leaked markup."
    policy: ["P2"]
    input: |-
      <think>ok</think><tool_call>
      <function=get_weather>
      <parameter=city>
      Par
    golden: [{"kind": "reasoning", "text": "ok"}]
    expect: {"vllm": {"verdict": "match", "note": "hypothesis: qwen3 tool parser drops the unterminated call; verify at capture time"}, "dynamo": {"verdict": "match", "note": "P2: v2 drops the partial trailing call, keeps reasoning"}}
  UNIFIED.two_calls.qwen3:
    description: "Two tool calls back-to-back, no reasoning (streamv2.2.a). Both must surface as ordered events."
    policy: []
    input: |-
      <tool_call>
      <function=f>
      <parameter=x>
      1
      </parameter>
      </function>
      </tool_call><tool_call>
      <function=g>
      <parameter=y>
      2
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "f", "arguments": {"x": "1"}}, {"kind": "tool_call", "name": "g", "arguments": {"y": "2"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.two_calls_same_name.qwen3:
    description: "The same tool called twice with different args (streamv2.2.d). Both calls are distinct events."
    policy: []
    input: |-
      <tool_call>
      <function=get_weather>
      <parameter=city>
      Paris
      </parameter>
      </function>
      </tool_call><tool_call>
      <function=get_weather>
      <parameter=city>
      Tokyo
      </parameter>
      </function>
      </tool_call>
    golden: [{"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Paris"}}, {"kind": "tool_call", "name": "get_weather", "arguments": {"city": "Tokyo"}}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "match"}}
  UNIFIED.two_reason_spans.qwen3:
    description: "Two reasoning spans separated by visible text, no tool call (REASONING.6.a). Streaming keeps both spans in order; batch merges them."
    policy: []
    input: |-
      <think>first thought</think>interlude <think>second thought</think>done
    golden: [{"kind": "reasoning", "text": "first thought"}, {"kind": "text", "text": "interlude "}, {"kind": "reasoning", "text": "second thought"}, {"kind": "text", "text": "done"}]
    expect: {"vllm": {"verdict": "match"}, "dynamo": {"verdict": "diverge", "class": "MERGE", "note": "batch v1 reasoning merges both spans into one leading event"}}
