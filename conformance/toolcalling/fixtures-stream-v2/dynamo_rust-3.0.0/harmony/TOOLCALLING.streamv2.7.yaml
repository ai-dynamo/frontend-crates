family: harmony
mode: streamv2
captured_with:
  dynamo_rust: 3.0.0
cases:
  TOOLCALLING.streamv2.7.a:
    unavailable: Dynamo v1 has no token-id ingestion path — the harmony parser is text-only (re-tokenizes text via the harmony encoding). See the harmony_text (text) row.
  TOOLCALLING.streamv2.7.b:
    unavailable: Dynamo v1 has no token-id ingestion path — the harmony parser is text-only (re-tokenizes text via the harmony encoding). See the harmony_text (text) row.
  TOOLCALLING.streamv2.7.c:
    unavailable: Dynamo v1 has no token-id ingestion path — the harmony parser is text-only (re-tokenizes text via the harmony encoding). See the harmony_text (text) row.
  TOOLCALLING.streamv2.7.d:
    unavailable: Dynamo v1 has no token-id ingestion path — the harmony parser is text-only (re-tokenizes text via the harmony encoding). See the harmony_text (text) row.
  TOOLCALLING.streamv2.7.e:
    unavailable: Dynamo v1 has no token-id ingestion path — the harmony parser is text-only (re-tokenizes text via the harmony encoding). See the harmony_text (text) row.
  TOOLCALLING.streamv2.7.f:
    unavailable: Dynamo v1 has no token-id ingestion path — the harmony parser is text-only (re-tokenizes text via the harmony encoding). See the harmony_text (text) row.
