# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# v1 jail golden/after fixture (generated by build_jail_fixtures.py).
# chunks = stream input; golden = jail output before #10570; now = after #10570.
# The tab is green when now == golden, red otherwise. Do not edit by hand.

family: phi4
model_label: Phi-4
mode: jail
cases:
  TOOLCALLING.jail.30.a:
    description: JSON-array call separator `,` inside one argument string value
    ref: derived from TOOLCALLING.batch.30.a
    chunks:
    - delta_text: 'functools[{"name": "run_query",'
    - delta_text: ' "arguments":'
    - delta_text: ' {"sql": "SELECT a,'
    - delta_text: ' SELECT b"}}]'
    - delta_text: ''
      finish_reason: stop
    golden:
      calls:
      - name: run_query
        arguments:
          sql: SELECT a, SELECT b
      normal_text: ''
    now:
      calls:
      - name: run_query
        arguments:
          sql: SELECT a, SELECT b
      normal_text: ''
  TOOLCALLING.jail.30.b:
    description: JSON structural delimiters `}` and `]` inside one argument string
      value
    ref: derived from TOOLCALLING.batch.30.b
    chunks:
    - delta_text: 'functools[{"name": "run_query",'
    - delta_text: ' "arguments":'
    - delta_text: ' {"sql": "SELECT value'
    - delta_text: ' WHERE note'
    - delta_text: ' has'
    - delta_text: ' brace }'
    - delta_text: ' and bracket ]'
    - delta_text: '"}}]'
    - delta_text: ''
      finish_reason: stop
    golden:
      calls:
      - name: run_query
        arguments:
          sql: SELECT value WHERE note has brace } and bracket ]
      normal_text: ''
    now:
      calls:
      - name: run_query
        arguments:
          sql: SELECT value WHERE note has brace } and bracket ]
      normal_text: ''
  TOOLCALLING.jail.30.c:
    description: Phi-4 marker-like text `func-tools` and `[]` inside one argument
      string value
    ref: derived from TOOLCALLING.batch.30.c
    chunks:
    - delta_text: 'functools[{"name": "run_query",'
    - delta_text: ' "arguments":'
    - delta_text: ' {"sql": "literal func-tools'
    - delta_text: ' marker[] stays'
    - delta_text: ' text"}}]'
    - delta_text: ''
      finish_reason: stop
    golden:
      calls:
      - name: run_query
        arguments:
          sql: literal func-tools marker[] stays text
      normal_text: ''
    now:
      calls:
      - name: run_query
        arguments:
          sql: literal func-tools marker[] stays text
      normal_text: ''
